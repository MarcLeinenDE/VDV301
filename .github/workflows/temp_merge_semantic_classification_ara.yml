name: temp-merge-semantic-classification-ara

on:
  push:
    branches:
      - dev/schema-integration
    paths:
      - .github/workflows/temp_merge_semantic_classification_ara.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  merge-ara:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout canonical branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install validation dependencies
        run: python -m pip install --disable-pip-version-check jsonschema lxml==6.1.2

      - name: Verify exact inputs and snapshot immutable state
        shell: bash
        run: |
          set -euo pipefail
          test "$(git hash-object audit_registry/finding_semantic_classification_v0.1.json)" = "b922804508a0d5c95f16c5f93af9fe0bf43f2f22"
          test "$(git hash-object audit_registry/finding_semantic_classification_schema_v0.1.json)" = "7cb69f527fe9d69e5b0fc59f6a23c36e0e426571"
          test "$(git hash-object tools/validate_finding_semantic_classification.py)" = "3304b31b2233e48d8d85e2ef9e248397d9a86b4e"
          test "$(git hash-object tools/merge_semantic_classification_block.py)" = "bddb17d5f264663611585cde33202c355ce9e5ca"
          test "$(git hash-object audit_registry/semantic_classification_blocks/ARA_v0.1.json)" = "a7fb4a3919f19de6ba0f87877a5587542393a842"
          test "$(git hash-object audit_registry/finding_provenance_baseline_2026-09-14.json)" = "5234317290e094962b9cde9662af856fe1fec6d1"
          test "$(git hash-object audit_registry/finding_inventory_frozen_2026-09-03.json)" = "02fe0d5f71f2b2674319d37f970ecd2b5bfe27cf"
          test "$(git hash-object audit_registry/finding_revalidation_registry_v0.1.json)" = "ab6d5f09081435e1d744b50af0a3a5f572eb5cef"
          test "$(git hash-object 00_START_HERE/CURRENT_STATE.json)" = "6e401f0bd374379e74ce2a065fab88f2016f9f67"
          test "$(find . -maxdepth 1 -type f -name '*.xsd' | wc -l)" -eq 50
          sha256sum ./*.xsd | sort -k2 > /tmp/ara-classification-xsd-before.sha256
          git diff --exit-code

      - name: Merge manually reviewed ARA block
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p evidence_out
          python tools/merge_semantic_classification_block.py \
            --block audit_registry/semantic_classification_blocks/ARA_v0.1.json \
            | tee evidence_out/merge.log
          grep -Fq "PASSED: merged block ARA; classified=10 remaining=182" evidence_out/merge.log

      - name: Validate Known-Issues registry
        shell: bash
        run: |
          set -euo pipefail
          python tools/validate_finding_semantic_classification.py | tee evidence_out/classification.log
          grep -Fq "PASSED: semantic classification registry valid; 10 reviewed entries anchored to frozen baseline" evidence_out/classification.log
          python - <<'PY'
          import json
          from pathlib import Path
          p=Path('audit_registry/finding_semantic_classification_v0.1.json')
          d=json.loads(p.read_text(encoding='utf-8'))
          assert d['state']=='in_progress'
          assert d['progress']['classified_count']==10
          assert d['progress']['remaining_count']==182
          assert d['progress']['reviewed_blocks']==['ARA']
          assert d['progress']['latest_block']=='ARA'
          ids=[e['finding_id'] for e in d['entries']]
          assert ids[:4]==['ARA-001','ARA-002','ARA-003','ARA-004']
          for fid in ('TVS-001','TVS-002','TVS-003','TSM-002','TSM-003','TSD-004'):
              assert fid in ids
          print('ARA_REGISTRY_POSTSTATE_OK')
          PY

      - name: Run complete 50-root-XSD regression pool
        shell: bash
        run: |
          set -euo pipefail
          python tools/validate_xsd_pool.py | tee evidence_out/xsd_pool.log
          grep -Fq "PASSED" evidence_out/xsd_pool.log

      - name: Enforce exact one-path mutation and immutability
        shell: bash
        run: |
          set -euo pipefail
          sha256sum ./*.xsd | sort -k2 > /tmp/ara-classification-xsd-after.sha256
          cmp /tmp/ara-classification-xsd-before.sha256 /tmp/ara-classification-xsd-after.sha256
          test "$(git hash-object audit_registry/finding_provenance_baseline_2026-09-14.json)" = "5234317290e094962b9cde9662af856fe1fec6d1"
          test "$(git hash-object audit_registry/finding_inventory_frozen_2026-09-03.json)" = "02fe0d5f71f2b2674319d37f970ecd2b5bfe27cf"
          test "$(git hash-object audit_registry/finding_revalidation_registry_v0.1.json)" = "ab6d5f09081435e1d744b50af0a3a5f572eb5cef"
          test "$(git hash-object 00_START_HERE/CURRENT_STATE.json)" = "6e401f0bd374379e74ce2a065fab88f2016f9f67"
          rm -rf evidence_out
          mapfile -t changed < <(git status --porcelain | sed 's/^...//' | sort)
          printf '%s\n' "${changed[@]}"
          test "${#changed[@]}" -eq 1
          test "${changed[0]}" = "audit_registry/finding_semantic_classification_v0.1.json"
          git diff --check
          if git status --porcelain | grep -E '\.xsd$'; then
            echo 'XSD mutation detected' >&2
            exit 1
          fi

      - name: Recreate evidence and upload
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p evidence_out
          python tools/validate_finding_semantic_classification.py > evidence_out/classification.log
          python tools/validate_xsd_pool.py > evidence_out/xsd_pool.log
          cp audit_registry/semantic_classification_blocks/ARA_v0.1.json evidence_out/ARA_v0.1.json
          python - <<'PY'
          import hashlib, json
          from pathlib import Path
          registry=Path('audit_registry/finding_semantic_classification_v0.1.json')
          manifest={
            'block':'ARA',
            'classified_count':10,
            'remaining_count':182,
            'registry_sha256':hashlib.sha256(registry.read_bytes()).hexdigest(),
            'frozen_baseline_payload_sha256':'d5bf14df302d57a0fc3d71792e1cb00583fafd3e9c7efd19721fd0de353dd576',
            'xsd_pool':'PASS',
            'xsd_mutation':False,
          }
          Path('evidence_out/manifest.json').write_text(json.dumps(manifest,indent=2)+'\n',encoding='utf-8')
          PY

      - name: Upload ARA classification evidence
        uses: actions/upload-artifact@v4
        with:
          name: semantic-classification-ara-evidence
          path: evidence_out/

      - name: Commit and push ARA classification registry
        shell: bash
        run: |
          set -euo pipefail
          rm -rf evidence_out
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add audit_registry/finding_semantic_classification_v0.1.json
          git diff --cached --check
          test "$(git diff --cached --name-only)" = "audit_registry/finding_semantic_classification_v0.1.json"
          git commit -m 'audit: merge ARA semantic classification'
          git push origin HEAD:dev/schema-integration
