name: TEMP validate COMMON V2.0 EV-136

on:
  push:
    branches: [dev/schema-integration]
    paths:
      - .github/workflows/temp_validate_common_v20_ev136.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install evidence dependencies
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y poppler-utils
          python -m pip install --disable-pip-version-check lxml

      - name: Fetch exact byte-pinned COMMON V2.0 PDF
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import hashlib
          import json
          from pathlib import Path
          import urllib.request
          source_id = 'COMMON_V2.0'
          sources = json.loads(Path('audit_registry/pdf_source_registry_v0.1.json').read_text(encoding='utf-8'))
          pins = json.loads(Path('audit_registry/pdf_source_pins_v0.1.json').read_text(encoding='utf-8'))
          source = next(x for x in sources['sources'] if x['source_id'] == source_id)
          pin = next(x for x in pins['sources'] if x['source_id'] == source_id)
          request = urllib.request.Request(source['official_url'], headers={
              'User-Agent': 'VDV301-Audit-EV136/0.1 (+https://github.com/MarcLeinenDE/VDV301)',
              'Accept': 'application/pdf,application/octet-stream;q=0.9,*/*;q=0.1',
          })
          with urllib.request.urlopen(request, timeout=90) as response:
              data = response.read()
          actual_hash = hashlib.sha256(data).hexdigest()
          assert data.startswith(b'%PDF-')
          assert actual_hash == pin['expected_sha256'], (actual_hash, pin['expected_sha256'])
          assert len(data) == int(pin['expected_size_bytes']), (len(data), pin['expected_size_bytes'])
          target = Path('local_sources/vdv_pdfs') / source['local_filename']
          target.parent.mkdir(parents=True, exist_ok=True)
          target.write_bytes(data)
          print(f'PIN_OK {source_id} sha256={actual_hash} size={len(data)}')
          PY

      - name: Run EV-136 Common V2.0 revalidation gate
        shell: bash
        env:
          EV136_OUTPUT_DIR: artifacts/ev136
        run: |
          set -euo pipefail
          python tools/validate_common_v20_revalidation_ev136.py

      - name: Render EV-136 page 13
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts/ev136/pages artifacts/ev136/text
          pdftoppm -f 13 -l 13 -singlefile -png -r 180 \
            local_sources/vdv_pdfs/COMMON_V2.0.pdf artifacts/ev136/pages/page-13
          pdftotext -f 13 -l 13 -layout \
            local_sources/vdv_pdfs/COMMON_V2.0.pdf artifacts/ev136/text/page-13.txt

      - name: Build evidence manifest
        shell: bash
        run: |
          set -euo pipefail
          sha256sum local_sources/vdv_pdfs/COMMON_V2.0.pdf > artifacts/ev136/pdf.sha256
          find artifacts/ev136 -type f ! -name manifest.sha256 -print0 | sort -z | xargs -0 sha256sum > artifacts/ev136/manifest.sha256
          cat artifacts/ev136/ev136_result.json
          cat artifacts/ev136/manifest.sha256

      - name: Root XSD pool regression gate
        shell: bash
        run: |
          set -euo pipefail
          python tools/validate_xsd_pool.py --repo-root . --dms-v24-tests

      - name: Verify no tracked mutation
        shell: bash
        run: |
          set -euo pipefail
          test "$(git hash-object audit_registry/finding_inventory_frozen_2026-09-03.json)" = "02fe0d5f71f2b2674319d37f970ecd2b5bfe27cf"
          test "$(git hash-object docs/pdf_xsd_semantic_audit/deep_read/COMMON_V2.0.md)" = "dd436b990a4a80d7c0c2768181a1c3d27befd049"
          test "$(git hash-object IBIS-IP_common_V2.0.xsd)" = "8608e3dcd665c197c34da7f6ec6af5a3758da164"
          test "$(git hash-object IBIS-IP_Enumerations_V2.0.xsd)" = "27e3c183b00381d959622d13c10543123af8eef6"
          test "$(git hash-object tools/validate_common_v20_ev118.py)" = "6db122b1726376b11ac24cfec68dbfbd758b079e"
          test "$(git hash-object tools/validate_common_v20_revalidation_ev136.py)" = "731db974d47e798b3487dd6572f5027683a06fce"
          git diff --exit-code -- '*.xsd'
          test -z "$(git status --porcelain --untracked-files=no)"

      - name: Upload EV-136 evidence
        uses: actions/upload-artifact@v4
        with:
          name: common-v20-ev136
          path: artifacts/ev136
          if-no-files-found: error
          retention-days: 90
