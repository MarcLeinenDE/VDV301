# Deep Read Pass 2 — VDV 301-1 V1.0 DE

Date: 2026-08-28
Document ID: `VDV301-1_V1.0_DE`
Publication: VDV-Schrift 301-1, 01/2014, Teil 1: Systemarchitektur
Official source: `https://www.vdv.de/vdv-301-1-ibis-ip-teil-1-systemarchitektur.pdfx`

## Deep-read state

`needs_visual_review`

The full embedded text layer was freshly read independently from the existing first-pass audit. The text layer is generally good, with line-break/soft-hyphen artifacts. Web PDF screenshots for the relevant later pages repeatedly returned cache-miss during this pass; therefore figure/table-heavy visual confirmation is not claimed complete and `exhaustive_read` is intentionally withheld.

No dedicated prior OCR text for this exact document was found in the available File Library search. Existing analytical/audit notes are not treated as OCR.

Preferred reading source for this pass:

`embedded_text_with_visual_confirmation_where_available`

## Fresh-reading conclusions

The fresh read independently confirms the existing architecture interpretation:

- Part 1 defines the IBIS-IP architecture; Part 2 defines the technical XML interfaces.
- the former IBIS master/slave concept is replaced by a service-oriented architecture;
- service and operation identity are architectural concepts independent of a specific physical device;
- every vehicle is treated as its own IBIS-IP system boundary;
- the document limits the presented architecture to non-safety-related systems and gives only architectural security requirements, not a concrete TLS/cipher/certificate profile;
- rapidly changing information is associated with UDP/multicast suitability, while reliable communication is associated with TCP/HTTP;
- XML is the service information exchange format and Part 2 supplies the technical structures;
- SNTP and RTP are named as conceivable additional protocols but are explicitly not specified in this 01/2014 edition; later VDV publications may and do refine those areas.

No existing ARCH-001..ARCH-008 conclusion from the first pass was contradicted by the fresh reading.

## New/strengthened audit candidates

### DR3011-001 — wrong internal cross-reference for System-Management

Classification: `pdf_cross_reference_error_candidate`
Confidence: high from native text; visual page confirmation still pending.

The document's table of contents and section headings define:

- 5.1.2 = System-Dokumentation
- 5.1.3 = System-Management

In the example introducing `SystemManagementService`, however, the text calls it an implementation of the System-Management functional component and refers to `5.1.2`.

This is internally inconsistent. The intended reference is very likely 5.1.3.

No XSD change follows from this finding.

### DR3011-002 — stale/conceptual operation names in architecture example

Classification: `pdf_example_terminology_mismatch_candidate`
Status: `finding_refined` after fresh VDV 301-2 V1.0 cross-check.
Confidence: high for the cross-document mismatch; no XSD defect indicated.

The Part 1 example names operations such as:

- `GetDeviceState`
- `GetSystemStatus`
- `SubscribeDeviceStatus`
- `SubscribeSystemStatus`
- corresponding unsubscribe names.

The fresh VDV 301-2 V1.0 interface read instead consistently specifies the SystemManagementService operation family as:

- `GetDeviceStatus`
- `SubscribeDeviceStatus`
- `UnsubscribeDeviceStatus`
- `GetServiceStatus`
- `SubscribeServiceStatus`
- `UnsubscribeServiceStatus`

The selected historical V1.0 `IBIS-IP_SystemManagementService_V1.0.xsd` aligns with the Part 2 Get response terminology through:

- `SystemManagementService.GetDeviceStatusResponse`
- `SystemManagementService.GetServiceStatusResponse`.

Therefore the Part 1 example is best treated as stale/conceptual architecture terminology. It is not evidence for changing the V1.0 XSD or inventing aliases during validation.

### DR3011-003 — duplicate `IBIS-IP` abbreviation entry

Classification: `minor_pdf_editorial_duplicate_candidate`
Confidence: high from native text; visual confirmation still pending.
Semantic impact: none identified.

The abbreviation table contains two consecutive `IBIS-IP` entries with slightly different wording of the expansion.

This is retained as a minor editorial observation and should not become an SDK conformance failure.

## Source comparison

### Original PDF/native extraction

Quality: good for prose and headings; extraction contains normal soft-hyphen/line-break artifacts.

### Visual original

Status: partial. PDF screenshot function was invoked, but the relevant later pages returned cache-miss in this session. The source remains official and textually readable, but this prevents an honest `exhaustive_read` label until the affected figures/tables can be visually revisited.

### Existing OCR

Dedicated OCR for this exact document: not found in the available File Library search.

### Existing first-pass audit

Compared only after the fresh read. The prior ARCH-001..ARCH-008 conclusions remain supported. DR3011-002 was refined only after the independent VDV 301-2 V1.0 fresh read established the final interface terminology.

## Remediation policy

No PR, mail to VDV, alias, schema edit or SDK override is proposed during Deep Read. Findings are recorded first; disposition will be decided separately with the project owner after the deep audit.
