# Base / General validation backlog addendum

Status: open technical validation tasks.

```text
BG-VB-001 Compile schema_pools/official/VDV-301-1.0/IBIS_IP_V1.0.xsd with all relative includes resolved inside the isolated pool.
BG-VB-002 Validate representative positive V1.0 aggregate-root samples for CIS/JIS/DMS/PCS/Ticketing operations.
BG-VB-003 Create a controlled negative/mixed-family test proving that original aggregate roots must not be combined with later same-path V1.0 revisions.
BG-VB-004 Verify resolver diagnostics when service+version is ambiguous without release_context.
```

No item is marked passed until actually executed locally.
